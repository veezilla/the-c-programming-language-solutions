#include <stdio.h>

/**
 * Experiment to find out what happens when prints's argument string contains
 * \c, where c is some character not listed above.
 */

main()
{
    printf("Experimenting with unknown escape sequence \q");
}